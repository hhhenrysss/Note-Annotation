
{
  "name": "cs510-final-project",
  "version": "0.0.1",
  "description": "Note taking web app",
  "engines": {
    "node": "12.x"
  },
  "scripts": {
    "start": "cd backend && npm install && node app.js",
    "build": "cd frontend && npm install && npm run build"
  },
  "cacheDirectories": [
    "backend/node_modules",
    "frontend/node_modules"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/hhhenrysss/note-annotation.git"
  },
  "devDependencies": {}
}